/*
 * config.js — конфигурация подключения к JSONBin
 * Сгенерировано мастером setup.html — 22.06.2026, 13:25:10
 *
 * ┌─────────────────────────────────────────────────────────────────────────┐
 * │ БЕЗОПАСНОСТЬ — ПРОЧТИ ПЕРЕД ДЕПЛОЕМ                                        │
 * │                                                                           │
 * │ Этот файл отдаётся клиенту как есть (его кэширует и Service Worker, см.    │
 * │ sw.js → APP_SHELL). Всё, что здесь лежит, ПУБЛИЧНО, если сайт публичный.   │
 * │                                                                           │
 * │ • ACCESS_KEY — нужен для чтения/обновления/создания бинов из браузера.     │
 * │   Это нормально держать в клиенте, НО он должен быть ОГРАНИЧЕННЫМ Access    │
 * │   Key (права Read + Update + Create, БЕЗ Delete и без доступа к аккаунту). │
 * │                                                                           │
 * │ • MASTER_KEY — даёт ПОЛНЫЙ доступ к аккаунту JSONBin (удаление любых       │
 * │   бинов, биллинг и т.д.). В ДЕПЛОЙ ЕГО КЛАСТЬ НЕЛЬЗЯ. Он нужен только       │
 * │   локально в setup.html при первичной генерации бинов. В деплое оставляй   │
 * │   MASTER_KEY: "".                                                          │
 * │                                                                           │
 * │ Как закрыть дыру (≈2 минуты в дашборде jsonbin.io):                        │
 * │   1. API Keys → создать новый Access Key с правами Read+Update+Create.     │
 * │   2. Старый ключ (он сейчас в обоих полях ниже) — РОТИРОВАТЬ/удалить:       │
 * │      он уже мог утечь, раз лежал в публичном бандле.                       │
 * │   3. Вписать новый ограниченный ключ в ACCESS_KEY, MASTER_KEY оставить "".  │
 * └─────────────────────────────────────────────────────────────────────────┘
 */

const CONFIG = {
  ENABLED: true,

  // TODO(безопасность): заменить на ОТДЕЛЬНЫЙ ограниченный Access Key и
  // ротировать значение ниже — сейчас здесь лежит мастер-ключ, что небезопасно
  // для публичного деплоя (см. шапку файла).
  ACCESS_KEY: "$2a$10$DzKMwzEZKpvZKpqb20L9ze6yNFy4H2CPta22yK37Pv5qjy2PmSJxa",

  // В ДЕПЛОЕ держи пустым. Заполняется только локально в setup.html для
  // первичного создания бинов. Если пусто — storage.js шлёт X-Access-Key.
  MASTER_KEY: "",

  BINS: {
    exercises: "6a390d83da38895dfeeaae66",
    user_dima: "6a390d84f5f4af5e291c35a2",
    user_natela: "6a390d84f5f4af5e291c35a3",
    templates_dima: "6a390d85da38895dfeeaae67",
    templates_natela: "6a390d85da38895dfeeaae68",
    workoutIndex_dima: "6a390d85da38895dfeeaae69",
    workoutIndex_natela: "6a390d86f5f4af5e291c35a4",
  },
};
